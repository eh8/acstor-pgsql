apiVersion: batch/v1
kind: Job
metadata:
  name: pgbench-init
  namespace: cnpg-lab
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: pgbench
          image: postgres:16
          env:
            - name: PGHOST
              value: acstor-cnpg-rw.cnpg-lab.svc
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: app-user
                  key: username
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-user
                  key: password
            - name: PGDATABASE
              value: app
          command:
            - bash
            - -lc
            - |
              pgbench --initialize --scale 10
